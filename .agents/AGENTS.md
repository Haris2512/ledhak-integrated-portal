# Project Guidelines & Git Workflow Rules

## Git Workflow Rule
- **Branch Protection**: Never commit or push directly to the `main` branch.
- **Branch-first Strategy**: Always work, commit, and push to dedicated feature branches or the `backend` branch first.
- **Merging**: Merging into `main` should only be performed when explicitly requested or after feature review.
